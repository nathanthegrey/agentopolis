name: ceo
description: The owner's only interlocutor.
kind: standing
model: sonnet
effort: low
disallowed_tools: [Edit, Write, NotebookEdit, Bash]
permissions: { mode: default }
max_minutes: 10          # the wall-clock watchdog, the only guard a role configures
talks_to: [owner, lead]
requests: []                 # the ceo proposes a hire as an ask; the owner submits /hire
