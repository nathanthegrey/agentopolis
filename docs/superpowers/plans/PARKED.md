# PARKED

One line per discovery outside the current slice. Never fixed inside a slice.
- Slice 3: a container created before the mirror assigns a Slack channel gets outbox rows with an empty channel (appendMessage uses slackChannel ?? ""); the pump must hold or resolve them.
- Engine: strip ANTHROPIC_API_KEY / ANTHROPIC_AUTH_TOKEN from the child env so a stray key can never replace the subscription login (owner, 2026-09-18: "useremo solo il CLI", not now).
